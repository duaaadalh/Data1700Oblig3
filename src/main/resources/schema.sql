CREATE TABLE Bestilling
(
    film VARCHAR (30) NOT NULL,
    antall VARCHAR (3) NOT NULL,
    fornavn VARCHAR (40) NOT NULL ,
    etternavn VARCHAR (40) NOT NULL ,
    tel VARCHAR (16) NOT NULL ,
    mail VARCHAR (30) NOT NULL ,
    PRIMARY KEY (mail)
);